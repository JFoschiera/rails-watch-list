<div class="row justify-content-center">
  <div class="col-3">
    <h3>Add List</h3>
    <%= simple_form_for [list] do |f| %>
      <%= f.input :name %>
      <%= f.button :submit, class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>
